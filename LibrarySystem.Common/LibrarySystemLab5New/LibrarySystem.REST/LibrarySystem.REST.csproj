<Project Sdk="Microsoft.NET.Sdk.Web">
	<PropertyGroup>
		<TargetFramework>net7.0</TargetFramework>
		<!--
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    -->
	</PropertyGroup>

	<ItemGroup>
		<!-- Ссылка на проект Infrastructure -->
		<ProjectReference Include="..\LibrarySystem.Infrastructure\LibrarySystem.Infrastructure.csproj" />

		<!-- JWT-авторизация -->
		<PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="7.0.0" />

		<!-- Swagger для тестирования -->
		<PackageReference Include="Swashbuckle.AspNetCore" Version="7.0.0" />

		<!-- EF Core Design (для миграций) -->
		<PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="7.0.0" />

		<!-- Пакет для SQLite, хотя сам провайдер берётся из Infrastructure -->
		<PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="7.0.0" />
	</ItemGroup>
</Project>
